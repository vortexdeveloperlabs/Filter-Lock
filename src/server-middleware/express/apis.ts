import type { Application } from "express";

import type { Options } from "FilterLockMiddleware";
import type { ModeHandler } from "FilterLockServerMiddlewareExpressTypes";

export function setupAPIs(app: Application, options: Options) {
  /*
  app.get(..., (req, resp) => {
      // TODO: ...
  });
  */
  // TODO: ...
}
